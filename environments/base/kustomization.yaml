# Shared standard applied to every environment: which Helm chart(s) to pull
# and the default values common everywhere. Per-environment overlays patch
# this, they don't redefine it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - istio-helm.yaml
